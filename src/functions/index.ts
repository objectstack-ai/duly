// Copyright (c) 2026 ObjectStack. Licensed under the Apache-2.0 license.
//
// Named callables a `script` flow node resolves by name at run time. A flow
// function is PURE: it takes `input` and RETURNS a value; a later declarative
// node persists it. Add yours to this map — the map itself is already wired
// into objectstack.config.ts, so no feature branch needs to touch the config.

export const dulyFunctions = {};
